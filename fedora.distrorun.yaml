version: "1"
name: "OsTestEmsi"

distro:
  base: fedora
  type: workstation   

users:
  - name: admin
    password: admin123
  - name: root
    password: toor

packages:
  - vim
  - curl
  - htop

services:
  enable:
    - NetworkManager
    - gdm             
    - sshd

build:
  output: disk         
  disk_size: 20G       
  sbom: false
