version: "1"
name: "MyFedoraOS"

distro:
  base: fedora
  type: server   # "server" (default) or "workstation" (adds GNOME desktop)

users:
  - name: admin
    password: changeme
  - name: root
    password: toor

packages:
  - vim
  - curl
  - htop

services:
  enable:
    - sshd

build:
  sbom: false
