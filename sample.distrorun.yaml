version: "1.0"
name: testOS

distro:
  base: alpine

packages:
  - nginx
  - vim
  - curl
  - openssh

users:
  - name: root
    password: toor
  - name: charif
    password: charif123

services:
  enable:
    - nginx
    - sshd
    - networking

build:
  output: disk
  disk_size: 20G
  sbom: true