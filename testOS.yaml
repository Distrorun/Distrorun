version: "1.0"
name: testOS

distro:
  base: fedora
  type: server

packages:
  - nginx
  - vim
  - curl

users:
  - name: root
    password: toor
  - name: charif
    password: charif123

services:
  enable:
    - nginx
    - sshd

build:
  output: iso
  sbom: false
